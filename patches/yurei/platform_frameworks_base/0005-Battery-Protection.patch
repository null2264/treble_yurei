From 8a4280d0c91e4373a8f7f968026b7d47a8334e4f Mon Sep 17 00:00:00 2001
From: Ahmad Ansori Palembani <46041660+null2264@users.noreply.github.com>
Date: Fri, 7 Aug 2026 17:02:05 +0700
Subject: [PATCH 1/9] feat: Battery Protect

Only works on Samsung since it uses Samsung's node
---
 .../android/server/BatteryProtectionUtil.java | 29 +++++++++++++++
 .../com/android/server/BatteryService.java    | 36 ++++++++++++++++++-
 2 files changed, 64 insertions(+), 1 deletion(-)
 create mode 100644 services/core/java/com/android/server/BatteryProtectionUtil.java

diff --git a/services/core/java/com/android/server/BatteryProtectionUtil.java b/services/core/java/com/android/server/BatteryProtectionUtil.java
new file mode 100644
index 0000000000000..2cc3baadd9a85
--- /dev/null
+++ b/services/core/java/com/android/server/BatteryProtectionUtil.java
@@ -0,0 +1,29 @@
+public class BatteryProtectionUtil {
+    /**
+     * Resolves and caches the supported sysfs node path.
+     */
+    private static String getSupportedNode() {
+        File node = new File("/sys/class/power_supply/battery/batt_slate_mode");
+        if (node.exists() && node.canWrite()) {
+            return "/sys/class/power_supply/battery/batt_slate_mode";
+        }
+        
+        return null;
+    }
+
+    public static void setChargingEnabled(boolean enable) {
+        String nodePath = getSupportedNode();
+        if (nodePath == null) {
+            return;
+        }
+
+        String value = enable ? "0" : "1";
+
+        try (FileWriter fw = new FileWriter(nodePath)) {
+            fw.write(value);
+            fw.flush();
+        } catch (IOException e) {
+            Slog.e(TAG, "Failed to write to charging node: " + nodePath, e);
+        }
+    }
+}
\ No newline at end of file
diff --git a/services/core/java/com/android/server/BatteryService.java b/services/core/java/com/android/server/BatteryService.java
index e6dec70b52eb3..33b93dd49bfe7 100644
--- a/services/core/java/com/android/server/BatteryService.java
+++ b/services/core/java/com/android/server/BatteryService.java
@@ -284,6 +284,7 @@ public final class BatteryService extends SystemService {
     private int mBatteryNearlyFullLevel;
     private int mShutdownBatteryTemperature;
     private boolean mShutdownIfNoPower;
+    private boolean mBatteryProtect;
 
     private static String sSystemUiPackage;
 
@@ -461,6 +462,7 @@ public BatteryService(Context context, @NonNull Looper looper) {
                 com.android.internal.R.bool.config_shutdownIfNoPower);
         sSystemUiPackage = mContext.getResources().getString(
                 com.android.internal.R.string.config_systemUi);
+        mBatteryProtect = false;
 
         mBatteryLevelsEventQueue = new ArrayDeque<>();
         mMetricsLogger = new MetricsLogger();
@@ -501,6 +503,16 @@ public void onBootPhase(int phase) {
         if (phase == PHASE_ACTIVITY_MANAGER_READY) {
             // check our power situation now that it is safe to display the shutdown dialog.
             synchronized (mLock) {
+                final ContentResolver resolver = mContext.getContentResolver();
+                ContentObserver obsProtection = new ContentObserver(mHandler) {
+                    @Override
+                    public void onChange(boolean selfChange) {
+                        synchronized (mLock) {
+                            updateBatteryProtect();
+                            processValuesLocked(true);
+                        }
+                    }
+                };
                 ContentObserver obs = new ContentObserver(mHandler) {
                     @Override
                     public void onChange(boolean selfChange) {
@@ -509,15 +521,23 @@ public void onChange(boolean selfChange) {
                         }
                     }
                 };
-                final ContentResolver resolver = mContext.getContentResolver();
+                resolver.registerContentObserver(Settings.System.getUriFor(
+                        "yurei_battery_protect"),
+                        false, obsProtection, UserHandle.USER_ALL);
                 resolver.registerContentObserver(Settings.Global.getUriFor(
                         Settings.Global.LOW_POWER_MODE_TRIGGER_LEVEL),
                         false, obs, UserHandle.USER_ALL);
+                updateBatteryProtect();
                 updateBatteryWarningLevelLocked();
             }
         }
     }
 
+    private void updateBatteryProtect() {
+        mBatteryProtect = Settings.System.getInt(
+            mContext.getContentResolver(), "yurei_battery_protect", 0) == 1;
+    }
+
     private void registerHealthCallback() {
         traceBegin("HealthInitWrapper");
         // IHealth is lazily retrieved.
@@ -796,6 +816,8 @@ private void processValuesLocked(boolean force) {
                 || mHealthInfo.chargingState != mLastBroadcastChargingState
                 || mHealthInfo.batteryCapacityLevel != mLastBroadcastBatteryCapacityLevel)) {
 
+            processBatteryProtectLocked()
+
             if (mPlugType != mLastBroadcastPlugType) {
                 if (mLastBroadcastPlugType == BATTERY_PLUGGED_NONE) {
                     // discharging -> charging
@@ -1611,6 +1633,18 @@ private void suspendBatteryInput() {
         mBatteryInputSuspended = true;
     }
 
+    private void processBatteryProtectLocked() {
+        if (mBatteryProtect && mHealthInfo.batteryLevel >= 80) {
+            BatteryProtectionUtil.setChargingEnabled(false);
+            return;
+        }
+
+        // Re-enable charging if battery drops below threshold (e.g., hysteresis at 75%) or if disabled
+        if (mHealthInfo.batteryLevel <= 75 || !mBatteryProtect) {
+            BatteryProtectionUtil.setChargingEnabled(true);
+        }
+    }
+
     private void processValuesLocked(boolean forceUpdate, @Nullable PrintWriter pw) {
         processValuesLocked(forceUpdate);
         if (pw != null && forceUpdate) {

From b3c4b6edf7f9ac0b1f5ab580f8d3d0928477725a Mon Sep 17 00:00:00 2001
From: Ahmad Ansori Palembani <46041660+null2264@users.noreply.github.com>
Date: Fri, 7 Aug 2026 17:08:24 +0700
Subject: [PATCH 2/9] fix: Clean up when charger is unplugged

---
 services/core/java/com/android/server/BatteryService.java | 6 +++++-
 1 file changed, 5 insertions(+), 1 deletion(-)

diff --git a/services/core/java/com/android/server/BatteryService.java b/services/core/java/com/android/server/BatteryService.java
index 33b93dd49bfe7..dab3c6dafe41e 100644
--- a/services/core/java/com/android/server/BatteryService.java
+++ b/services/core/java/com/android/server/BatteryService.java
@@ -1634,12 +1634,16 @@ private void suspendBatteryInput() {
     }
 
     private void processBatteryProtectLocked() {
+        if (mPlugType == BATTERY_PLUGGED_NONE) {
+            BatteryProtectionUtil.setChargingEnabled(true);
+            return;
+        }
+
         if (mBatteryProtect && mHealthInfo.batteryLevel >= 80) {
             BatteryProtectionUtil.setChargingEnabled(false);
             return;
         }
 
-        // Re-enable charging if battery drops below threshold (e.g., hysteresis at 75%) or if disabled
         if (mHealthInfo.batteryLevel <= 75 || !mBatteryProtect) {
             BatteryProtectionUtil.setChargingEnabled(true);
         }

From 16fad671cd467bb2d59c74368376aa243d7d65ec Mon Sep 17 00:00:00 2001
From: Ahmad Ansori Palembani <46041660+null2264@users.noreply.github.com>
Date: Fri, 7 Aug 2026 17:09:46 +0700
Subject: [PATCH 3/9] fix: Missing stuff

---
 .../java/com/android/server/BatteryProtectionUtil.java     | 7 +++++++
 services/core/java/com/android/server/BatteryService.java  | 2 +-
 2 files changed, 8 insertions(+), 1 deletion(-)

diff --git a/services/core/java/com/android/server/BatteryProtectionUtil.java b/services/core/java/com/android/server/BatteryProtectionUtil.java
index 2cc3baadd9a85..48d9db0be5c73 100644
--- a/services/core/java/com/android/server/BatteryProtectionUtil.java
+++ b/services/core/java/com/android/server/BatteryProtectionUtil.java
@@ -1,3 +1,10 @@
+package com.android.server;
+
+import android.util.Slog;
+import java.io.File;
+import java.io.FileWriter;
+import java.io.IOException;
+
 public class BatteryProtectionUtil {
     /**
      * Resolves and caches the supported sysfs node path.
diff --git a/services/core/java/com/android/server/BatteryService.java b/services/core/java/com/android/server/BatteryService.java
index dab3c6dafe41e..a8da703163ff1 100644
--- a/services/core/java/com/android/server/BatteryService.java
+++ b/services/core/java/com/android/server/BatteryService.java
@@ -816,7 +816,7 @@ private void processValuesLocked(boolean force) {
                 || mHealthInfo.chargingState != mLastBroadcastChargingState
                 || mHealthInfo.batteryCapacityLevel != mLastBroadcastBatteryCapacityLevel)) {
 
-            processBatteryProtectLocked()
+            processBatteryProtectLocked();
 
             if (mPlugType != mLastBroadcastPlugType) {
                 if (mLastBroadcastPlugType == BATTERY_PLUGGED_NONE) {

From 22df548c816985841e92aa2972d3ff90796ebac8 Mon Sep 17 00:00:00 2001
From: Ahmad Ansori Palembani <46041660+null2264@users.noreply.github.com>
Date: Sun, 16 Aug 2026 16:35:00 +0700
Subject: [PATCH 4/9] fix: Fix infinite loop

---
 .../java/com/android/server/BatteryService.java   | 15 ++++++++++++---
 1 file changed, 12 insertions(+), 3 deletions(-)

diff --git a/services/core/java/com/android/server/BatteryService.java b/services/core/java/com/android/server/BatteryService.java
index a8da703163ff1..73493b2c6c8b3 100644
--- a/services/core/java/com/android/server/BatteryService.java
+++ b/services/core/java/com/android/server/BatteryService.java
@@ -285,6 +285,7 @@ public final class BatteryService extends SystemService {
     private int mShutdownBatteryTemperature;
     private boolean mShutdownIfNoPower;
     private boolean mBatteryProtect;
+    private boolean mChargeDisabled;
 
     private static String sSystemUiPackage;
 
@@ -463,6 +464,7 @@ public BatteryService(Context context, @NonNull Looper looper) {
         sSystemUiPackage = mContext.getResources().getString(
                 com.android.internal.R.string.config_systemUi);
         mBatteryProtect = false;
+        mChargeDisabled = false;
 
         mBatteryLevelsEventQueue = new ArrayDeque<>();
         mMetricsLogger = new MetricsLogger();
@@ -1634,18 +1636,25 @@ private void suspendBatteryInput() {
     }
 
     private void processBatteryProtectLocked() {
-        if (mPlugType == BATTERY_PLUGGED_NONE) {
+        if (!mBatteryProtect && mChargeDisabled) {
+            mChargeDisabled = false;
             BatteryProtectionUtil.setChargingEnabled(true);
             return;
         }
 
         if (mBatteryProtect && mHealthInfo.batteryLevel >= 80) {
-            BatteryProtectionUtil.setChargingEnabled(false);
+            if (!mChargeDisabled) {
+                mChargeDisabled = true;
+                BatteryProtectionUtil.setChargingEnabled(false);
+            }
             return;
         }
 
         if (mHealthInfo.batteryLevel <= 75 || !mBatteryProtect) {
-            BatteryProtectionUtil.setChargingEnabled(true);
+            if (mChargeDisabled) {
+                mChargeDisabled = false;
+                BatteryProtectionUtil.setChargingEnabled(true);
+            }
         }
     }
 

From 6ab2cd707a83d10864f368c61b646937657f3e54 Mon Sep 17 00:00:00 2001
From: Ahmad Ansori Palembani <46041660+null2264@users.noreply.github.com>
Date: Sun, 16 Aug 2026 16:52:58 +0700
Subject: [PATCH 5/9] refactor: Reduce IO usage

---
 .../android/server/BatteryProtectionUtil.java    | 16 ++++++++++++----
 1 file changed, 12 insertions(+), 4 deletions(-)

diff --git a/services/core/java/com/android/server/BatteryProtectionUtil.java b/services/core/java/com/android/server/BatteryProtectionUtil.java
index 48d9db0be5c73..6a7bff0d44b73 100644
--- a/services/core/java/com/android/server/BatteryProtectionUtil.java
+++ b/services/core/java/com/android/server/BatteryProtectionUtil.java
@@ -6,21 +6,29 @@
 import java.io.IOException;
 
 public class BatteryProtectionUtil {
+    private static final String TAG = "BatteryProtectionUtil";
+    private static String sSupportedNode = null;
+
     /**
      * Resolves and caches the supported sysfs node path.
      */
     private static String getSupportedNode() {
+        if (sSupportedNode != null) {
+            return sSupportedNode;
+        }
+
         File node = new File("/sys/class/power_supply/battery/batt_slate_mode");
         if (node.exists() && node.canWrite()) {
-            return "/sys/class/power_supply/battery/batt_slate_mode";
+            sSupportedNode = "/sys/class/power_supply/battery/batt_slate_mode";
+        } else {
+            sSupportedNode = "";
         }
-        
-        return null;
+        return sSupportedNode;
     }
 
     public static void setChargingEnabled(boolean enable) {
         String nodePath = getSupportedNode();
-        if (nodePath == null) {
+        if (sSupportedNode.isEmpty()) {
             return;
         }
 

From b92c5ce01c728a39d4080e977e40f02bfb273f50 Mon Sep 17 00:00:00 2001
From: Ahmad Ansori Palembani <46041660+null2264@users.noreply.github.com>
Date: Sun, 16 Aug 2026 16:53:49 +0700
Subject: [PATCH 6/9] refactor: Clean up code and add some sanity checks

---
 .../com/android/server/BatteryService.java    | 25 +++++++++----------
 1 file changed, 12 insertions(+), 13 deletions(-)

diff --git a/services/core/java/com/android/server/BatteryService.java b/services/core/java/com/android/server/BatteryService.java
index 73493b2c6c8b3..51e38f4e79fdf 100644
--- a/services/core/java/com/android/server/BatteryService.java
+++ b/services/core/java/com/android/server/BatteryService.java
@@ -1636,25 +1636,24 @@ private void suspendBatteryInput() {
     }
 
     private void processBatteryProtectLocked() {
-        if (!mBatteryProtect && mChargeDisabled) {
-            mChargeDisabled = false;
-            BatteryProtectionUtil.setChargingEnabled(true);
+        if (!mBatteryProtect) {
+            if (mChargeDisabled) {
+                BatteryProtectionUtil.setChargingEnabled(true);
+                mChargeDisabled = false;
+            }
             return;
         }
 
-        if (mBatteryProtect && mHealthInfo.batteryLevel >= 80) {
-            if (!mChargeDisabled) {
-                mChargeDisabled = true;
-                BatteryProtectionUtil.setChargingEnabled(false);
-            }
+        if (mHealthInfo == null) {
             return;
         }
 
-        if (mHealthInfo.batteryLevel <= 75 || !mBatteryProtect) {
-            if (mChargeDisabled) {
-                mChargeDisabled = false;
-                BatteryProtectionUtil.setChargingEnabled(true);
-            }
+        if (mHealthInfo.batteryLevel >= 80 && !mChargeDisabled) {
+            BatteryProtectionUtil.setChargingEnabled(false);
+            mChargeDisabled = true;
+        } else if (mHealthInfo.batteryLevel <= 75 && mChargeDisabled) {
+            BatteryProtectionUtil.setChargingEnabled(true);
+            mChargeDisabled = false;
         }
     }
 

From 179a990ce7adde6f2177e062c320cf25c187e5ce Mon Sep 17 00:00:00 2001
From: Ahmad Ansori Palembani <46041660+null2264@users.noreply.github.com>
Date: Sun, 16 Aug 2026 16:57:59 +0700
Subject: [PATCH 7/9] fix: Check the wrong variable

---
 .../core/java/com/android/server/BatteryProtectionUtil.java     | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/services/core/java/com/android/server/BatteryProtectionUtil.java b/services/core/java/com/android/server/BatteryProtectionUtil.java
index 6a7bff0d44b73..9da6d5477538c 100644
--- a/services/core/java/com/android/server/BatteryProtectionUtil.java
+++ b/services/core/java/com/android/server/BatteryProtectionUtil.java
@@ -28,7 +28,7 @@ private static String getSupportedNode() {
 
     public static void setChargingEnabled(boolean enable) {
         String nodePath = getSupportedNode();
-        if (sSupportedNode.isEmpty()) {
+        if (nodePath.isEmpty()) {
             return;
         }
 

From cb9cbcd0a25c4676a5b2c63b04f9aa59630b8089 Mon Sep 17 00:00:00 2001
From: Ahmad Ansori Palembani <46041660+null2264@users.noreply.github.com>
Date: Sun, 16 Aug 2026 17:02:46 +0700
Subject: [PATCH 8/9] fix: System -> Global

---
 services/core/java/com/android/server/BatteryService.java | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

diff --git a/services/core/java/com/android/server/BatteryService.java b/services/core/java/com/android/server/BatteryService.java
index 51e38f4e79fdf..108888dc84e3b 100644
--- a/services/core/java/com/android/server/BatteryService.java
+++ b/services/core/java/com/android/server/BatteryService.java
@@ -523,7 +523,7 @@ public void onChange(boolean selfChange) {
                         }
                     }
                 };
-                resolver.registerContentObserver(Settings.System.getUriFor(
+                resolver.registerContentObserver(Settings.Global.getUriFor(
                         "yurei_battery_protect"),
                         false, obsProtection, UserHandle.USER_ALL);
                 resolver.registerContentObserver(Settings.Global.getUriFor(
@@ -536,7 +536,7 @@ public void onChange(boolean selfChange) {
     }
 
     private void updateBatteryProtect() {
-        mBatteryProtect = Settings.System.getInt(
+        mBatteryProtect = Settings.Global.getInt(
             mContext.getContentResolver(), "yurei_battery_protect", 0) == 1;
     }
 

From bc62707783bb6b72a9e6aff051d9883dcd736afe Mon Sep 17 00:00:00 2001
From: Ahmad Ansori Palembani <46041660+null2264@users.noreply.github.com>
Date: Tue, 18 Aug 2026 15:51:43 +0700
Subject: [PATCH 9/9] fix: Realign charge state

---
 services/core/java/com/android/server/BatteryService.java | 7 +++++++
 1 file changed, 7 insertions(+)

diff --git a/services/core/java/com/android/server/BatteryService.java b/services/core/java/com/android/server/BatteryService.java
index 108888dc84e3b..d7671afac86c7 100644
--- a/services/core/java/com/android/server/BatteryService.java
+++ b/services/core/java/com/android/server/BatteryService.java
@@ -1654,6 +1654,13 @@ private void processBatteryProtectLocked() {
         } else if (mHealthInfo.batteryLevel <= 75 && mChargeDisabled) {
             BatteryProtectionUtil.setChargingEnabled(true);
             mChargeDisabled = false;
+            return;  // No need to re-evaluate, we did want to enable the charge
+        }
+
+        // Samsung's firmware simply ignore the charge status and just let the phone charges.
+        // Simply telling it to disable it again fixes it.
+        if (mChargeDisabled && mHealthInfo.batteryStatus == BatteryManager.BATTERY_STATUS_CHARGING) {
+            BatteryProtectionUtil.setChargingEnabled(false);
         }
     }
 
